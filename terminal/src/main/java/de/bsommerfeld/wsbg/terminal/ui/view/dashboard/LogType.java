package de.bsommerfeld.wsbg.terminal.ui.view.dashboard;

/** Classification for terminal log entries, controlling color and layout. */
public enum LogType {
    INFO, ERROR, WARN, DEFAULT, STREAMED, CLEANUP, REDDIT, SYSTEM
}
