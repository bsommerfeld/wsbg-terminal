# HANDOFF — Post-Smoke-3-Fixes: Live-Config, EUR/USD-Joker, Ticker-Chip, JSON-Defer — 2026-06-23 13:30

## Kontext
Folge von `2026-06-23_00-30.md`. Smoke 3 lief sauber (Joker 0×503, Venue korrekt,
Metal-Default flüssig). Aus User-Beobachtungen danach fünf Punkte — alle adressiert.
**Alles kompiliert, gesamte Testsuite grün; nichts committet.**

## Erledigt (✅)

- ✅ **Live-Config-Bug** (Settings wirkten nicht live): `EditorialAgent` cachte
  `clusterThemeEnabled`/`newsCoverageEnabled`/`contextTtlMinutes` im Konstruktor →
  TICKER⇄ALLES wirkte erst nach Neustart. Jetzt **pro Tick live aus `config` gelesen**.
  39 Agent-Tests grün (inkl. `EditorialThemeTickTest`).
- ✅ **Q4 — EUR/USD über den Joker:** `EurUsdClient` nutzt jetzt den geteilten
  `WebFetcher` (browser→direct) statt eigenem `HttpClient` (currency-Modul bekam
  `source-api`-Dep). Yahoo `v8/chart/EURUSD=X` mit Browser-Fingerprint → kein 429 mehr;
  Frankfurter bleibt Floor. 19 currency-Tests grün.
- ✅ **Q6 — Ticker-Chip in der Headline-Karte:** `reddit.js`/`widgets.css` zeigen den
  aufgelösten Ticker als eigenen Chip (`.ticker-chip`, amber, führend in der Meta-Zeile),
  damit die Karte immer sagt, zu WELCHEM Instrument der Preis gehört — auch wenn der Text
  ihn nicht nennt. Kein Ticker (Yahoo löst nichts auf) → kein Chip (graceful).
- ✅ **Q2 — JSON-Kommentare defern:** `RedditScraper` holt Kommentar-Bäume nicht mehr
  inline beim Scan, sondern über einen Daemon-`commentQueue`-Worker + `pendingResurface`
  + `drainResurfaced` — exakt das RSS-Muster. Cold-Start so schnell wie RSS, volle
  Fidelity bleibt. 58 reddit-Tests grün (RedditScraperTest testet bewusst keine
  HTTP-/Kommentar-Methoden).
  - 🔧 **Verifikation offen:** deferred Kommentare + Resurface live auf dem JSON-Pfad
    gegentesten (aktuell IP-throttled, kein Live-JSON-Scan möglich). Unit-getestet +
    kompiliert; live-Soak ausstehend.

## Beantwortet (kein Code)
- **Q2 RSS+JSON verbinden?** Nein — getrennt halten (zwei Protokolle); nur das
  Defer-Verhalten vereinheitlicht (jetzt beide).
- **Q3 Mood-Scoring:** aus dem KI-`sentiment` pro Headline, über 24h. bullish=BULLISH/
  FOMO/BREAKOUT/SQUEEZE, bearish=BEARISH/CAPITULATION, NEUTRAL/MIXED/REVERSAL exkludiert.
  DAX-Memes waren NEUTRAL → kein Badge (korrekt).
- **Q2 „nichts mehr":** nicht der ALLES-Switch (wirkte eh nicht live), sondern ein
  Cold-Start-Riesen-Tick (27 Cluster, 12 dirty Units), der beim Fenster-Schließen mitten
  in der Compose-Phase abgebrochen wurde. Mit dem JSON-Defer + ruhigerem Cold-Start
  sollte das künftig schneller durchlaufen.
- **Q6 Mitteln?** Nein — adidas+DAX sind zwei Instrumente/Preise; mitteln = falscher
  Ticker. Grounding (Ticker+Preis) steht schon im `unitBrief`; das Problem war Evidenz-
  Fehlzuordnung (adidas bekam DAX-Meme-Evidenz). Der Ticker-Chip macht die Karte nun
  wenigstens eindeutig. (Editoriale Phantom-Unit-Vermeidung bleibt optional offen.)
- **Unverified-Preis-Badge:** Verkabelung ok; im Smoke hatten alle Headlines einen
  Yahoo-Snapshot (`priceVerified=true`) → nichts zu markieren → kein „!". Greift nur,
  wenn eine Zahl in der Zeile NICHT von Yahoo gedeckt ist.

## Offen / als Nächstes
- 🔧 Smoke 4: Live verifizieren — ALLES wirkt jetzt live; EUR/USD via Joker (Yahoo statt
  Frankfurter); Ticker-Chip sichtbar; JSON-Cold-Start schneller (deferred Kommentare).
- ⏸️ Editoriale Phantom-Unit-Vermeidung (Compose-Selbstcheck/Extraktions-Disziplin) —
  vom User vorerst NICHT gewünscht; Ticker-Chip ist die visuelle Zwischenlösung.
- ⏸️ In-App-Update paketiert verifizieren; idle Fetch-Browser entsorgen (RAM).
