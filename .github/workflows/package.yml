name: Package Native Launcher

on:
  release:
    types: [published]

permissions:
  contents: write

jobs:
  package-launcher:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            jpackage-type: msi
            icon-file: .assets/icons/AppIcon.ico
          - os: macos-latest
            jpackage-type: dmg
            icon-file: .assets/icons/AppIcon.icns
          - os: ubuntu-latest
            jpackage-type: deb
            icon-file: .assets/icons/AppIcon.png

    runs-on: ${{ matrix.os }}
    name: Build ${{ matrix.jpackage-type }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'zulu'
          cache: maven

      - name: Build Launcher JAR
        run: mvn clean package -pl launcher -am -DskipTests

      - name: Create JPackage input directory
        shell: bash
        run: |
          mkdir -p jpackage-input
          cp launcher/target/launcher-*.jar jpackage-input/

      - name: Package with JPackage
        shell: bash
        run: |
          jpackage \
            --type ${{ matrix.jpackage-type }} \
            --name "WSBG Terminal" \
            --app-version "${{ github.event.release.tag_name }}" \
            --vendor "bsommerfeld" \
            --description "WSBG Terminal Launcher" \
            --input jpackage-input \
            --main-jar launcher-1.0.0.jar \
            --main-class de.bsommerfeld.updater.launcher.LauncherMain \
            --java-options "--enable-preview" \
            --icon ${{ matrix.icon-file }} \
            --dest jpackage-output

      - name: Upload Installer to Release
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload "${{ github.event.release.tag_name }}" \
            jpackage-output/* \
            --clobber
