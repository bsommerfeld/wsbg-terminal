# Umgesetzt: #1 Compose pro Subjekt + #5 Uncap — 2026-06-05 03:26

## Kontext

Über drei Läufe bewiesen: gemma4:e4b (4B) bricht beim **einen großen Batch-JSON**
zusammen — mal abgeschnitten, mal degeneriert (5× `*Hypothetical*`), mal komplett
Prosa. Die Intelligenz ist da, die Struktur-Compliance nicht. Fix: nie batchen.

## Erledigt

✅ **#1 Compose pro Subjekt** (`EditorialAgent`)
- Neuer Prompt **`headline-compose-single.txt`**: Persona + Faithfulness +
  **Highlight-Rubrik verbatim** übernommen, aber: **EIN** Subjekt rein, **EIN**
  JSON-Objekt raus, striktes „**nur ein JSON-Objekt, keine Code-Fences/Prosa/
  Markdown**". `headline-compose.txt` (Batch) bleibt liegen, ist aber tot.
- `composeOne(model, brief, subject)` — ein fokussierter Call je Subjekt; ein
  subjektloser Cluster bekommt EINE „general sentiment"-Zeile (`subject = null`).
- Parsing forgiving: Single-Object → `{"headlines":[…]}`-Wrapper (Modell-Marotte)
  → sonst erstes balanciertes `{…}` aus `salvageObjects` (string-/klammer-bewusst).
- `processCluster` ruft `composeOne` pro aufgelöstem Subjekt; QA + Persist je Draft.

✅ **#5 Subjekte uncappen** — `MAX_SUBJECTS=6` raus; `extractSubjects` nimmt alle.

✅ **Related-Bremse** — `resolve(name, withRelated)`: nur die **ersten 6** Subjekte
  ziehen den teuren Second-Hop (related charts+news); der Rest nur Ticker+News.
  Hält die Yahoo-Last beim Uncappen im Zaum (`RELATED_SUBJECT_LIMIT`).

✅ **Lab-Trace umgebaut** — `ClusterEditorial.subjectDrafts` (List<SubjectDraft>:
  label, draft|null, salvaged, raw, ms). Zeigt **pro Subjekt** eine Zeile mit
  Headline + Einzel-Latenz; bei „keine Headline" den Roh-Output (leer `""` =
  „nichts zu sagen" vs. kaputt). Timing-Zeile zeigt jetzt auch Call-Anzahl.

Build: agent 106 Tests grün, voller Reactor grün.

## Was das löst

- **Degeneration / Truncation / Prosa** → weg (kein Batch mehr).
- **Quellen-Verbrennen** → sauber: jede Headline zitiert nur die Quellen IHRES
  Subjekts.
- **Nvidia-Halluzination** → strukturell weg: nur über aufgelöste Subjekte
  komponiert, Ticker nur wenn DIESES Subjekt einen hat.
- **„Subjekt = Einheit, nicht Ticker"** → erfüllt: theme/person/nothing bekommen
  genauso eine Zeile wie ein Instrument.

## Achtung / zu beobachten beim nächsten Lauf

- **Latenz**: N kleine Calls statt 1 großer. Bei 20+ Subjekten viele compose-Calls
  → langer Lauf. Der Trace zeigt jetzt ms pro Subjekt + Call-Anzahl → das ist die
  Datengrundlage für die Performance-Bewertung.
- **Yahoo-Last** steigt durch Uncap (N search + N chart, related auf 6 gebremst)
  → genau der nächste Schritt: **Yahoo-Limits testen** (429 ist real, siehe
  2026-06-05_02-47).
- Der ganze Brief geht (noch) in JEDEN Per-Subjekt-Call (mehr Tokens). Der
  Subjekt-Slice-Kontext kommt erst mit dem aufgeschobenen #2.

## Offen / als Nächstes

**Yahoo-Limits testen** (wie vereinbart), dann ggf. Related-Kompromiss/Batch
final. #2 (Subjekt-Slice-Kontext) bleibt aufgeschoben.
