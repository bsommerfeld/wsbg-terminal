# Umgesetzt: #9 Username-Zensur + Stage-Timing — 2026-06-05 02:53

## Kontext

Erster Umsetzungsschritt aus dem Plan (2026-06-05_02-37). Risikoarm, aktuelle
Struktur unangetastet.

## Erledigt

✅ **#9 Username-Zensur** (`ReportBuilder`)
- Kommentar-**Autor** wird im Brief durch `[user]` ersetzt (Konstante
  `ANON_AUTHOR`). Quellen werden weiter über IDs (`t1_…`) zitiert → kein Verlust.
- `u/handle` / `/u/handle`-**Erwähnungen im Text** (Kommentar-Body + Post-Snippet)
  werden via Regex `(?i)(?<![A-Za-z0-9])/?u/[A-Za-z0-9_-]{3,20}` ebenfalls zu
  `[user]`. Das killt die wahrscheinliche Quelle von `$NASX` (Handle → Ticker).
- Thread-Autor war ohnehin nie im Brief.
- Tests: `ReportBuilderTest` angepasst (Handle darf NICHT erscheinen, Body/Score/
  ID schon) + neuer Test für Handle-im-Body. Agent-Suite 100 grün.

✅ **Stage-Timing im Lab**
- `EditorialAgent.processCluster` misst pro Cluster `subjectsMs / resolveMs /
  composeMs` (nanoTime), trägt sie im `ClusterEditorial`-Record.
- `LabRunner` zeigt im Trace:
  `timing : subjects 4.2s · resolve 38.1s · compose 6.7s (total 49.0s)`.
- Damit ist die Performance-Frage zu #1 (Per-Subjekt-Compose) **messbar**: wir
  sehen vorher/nachher, was die Umstellung auf N kleine Calls kostet.

## Nutzen für die nächsten Schritte

- Beim nächsten Lab-Lauf siehst du, **wie teuer `resolve` wirklich ist** (Yahoo-
  Calls) vs. `compose` — relevant für die Uncap-/Related-/Batch-Entscheidung.
- Username-Halluzinationen ($NASX) sollten verschwinden.

## Offen / als Nächstes

Unverändert: **#8 Börsen-Präferenz** (Korrektheit; News bleiben gleich, siehe
2026-06-05_02-47) — separat #8b News-über-Ticker als kleines Experiment. Danach
**#1 Compose pro Subjekt + #5 Uncap**, dann Yahoo-Limits → Related-Kompromiss.
