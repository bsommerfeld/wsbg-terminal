<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>wsbg-terminal</artifactId>
        <groupId>de.bsommerfeld</groupId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>agent</artifactId>

    <dependencies>
         <dependency>
            <groupId>de.bsommerfeld</groupId>
            <artifactId>reddit</artifactId>
        </dependency>
        <dependency>
            <groupId>de.bsommerfeld</groupId>
            <artifactId>instrument-corpus</artifactId>
        </dependency>
        <dependency>
            <groupId>de.bsommerfeld</groupId>
            <artifactId>source-api</artifactId>
        </dependency>
        <dependency>
            <groupId>de.bsommerfeld</groupId>
            <artifactId>aggregator</artifactId>
        </dependency>
        <dependency>
            <groupId>de.bsommerfeld</groupId>
            <artifactId>signals</artifactId>
        </dependency>
        <dependency>
            <groupId>de.bsommerfeld</groupId>
            <artifactId>yahoo-finance</artifactId>
        </dependency>
        <dependency>
            <groupId>de.bsommerfeld</groupId>
            <artifactId>fear-greed</artifactId>
        </dependency>
        <dependency>
            <groupId>de.bsommerfeld</groupId>
            <artifactId>briefing</artifactId>
        </dependency>
        <dependency>
            <groupId>de.bsommerfeld</groupId>
            <artifactId>websearch</artifactId>
        </dependency>
        <dependency>
            <groupId>de.bsommerfeld</groupId>
            <artifactId>edgar</artifactId>
        </dependency>
        <!-- The KI-DD's German data legs, TEST-ONLY: the live DeepDiveSmokeIT
             wires the real clients against the real model; production wiring
             stays in the terminal module (AgentPipelineModule). -->
        <dependency>
            <groupId>de.bsommerfeld</groupId>
            <artifactId>consorsbank</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- COMPILE scope since 2026-08-03: the profile leg still arrives
             through the bound InstrumentFactsSource, but the DD now names
             onvista's own fundamentals types (ownership, benchmark fit,
             consensus drift) directly. -->
        <dependency>
            <groupId>de.bsommerfeld</groupId>
            <artifactId>onvista</artifactId>
        </dependency>
        <dependency>
            <groupId>de.bsommerfeld</groupId>
            <artifactId>tradingview</artifactId>
        </dependency>
        <dependency>
            <groupId>de.bsommerfeld</groupId>
            <artifactId>stocknear</artifactId>
        </dependency>
        <!-- The 2026-08-02 source wave's MARKET/FACT clients (no NewsSource, so
             they never reach the DD through the aggregator): CNBC quotes +
             earnings surprise history, boerse.de directors' dealings / analyst
             archive / 8 fiscal years, Handelsblatt ISIN master data,
             Boersenmedien instrument resolver, Traders Union movers. Compile
             scope on purpose - DeepDiveService itself names these types. -->
        <dependency>
            <groupId>de.bsommerfeld</groupId>
            <artifactId>cnbc</artifactId>
        </dependency>
        <dependency>
            <groupId>de.bsommerfeld</groupId>
            <artifactId>boerse-de</artifactId>
        </dependency>
        <dependency>
            <groupId>de.bsommerfeld</groupId>
            <artifactId>handelsblatt</artifactId>
        </dependency>
        <dependency>
            <groupId>de.bsommerfeld</groupId>
            <artifactId>boersenmedien</artifactId>
        </dependency>
        <dependency>
            <groupId>de.bsommerfeld</groupId>
            <artifactId>tradersunion</artifactId>
        </dependency>
        <!-- 2026-08-03: the regime gauges the DD reads by name (EUR/USD and
             the dollar index; Fear & Greed already rides the fear-greed dep). -->
        <dependency>
            <groupId>de.bsommerfeld</groupId>
            <artifactId>currency</artifactId>
        </dependency>
        <!-- 2026-08-03: the quote page's own profile figures, a second
             independent gap filler beside the CNBC quote. -->
        <dependency>
            <groupId>de.bsommerfeld</groupId>
            <artifactId>fool</artifactId>
        </dependency>
        <!-- 2026-08-03: the DD names finanzen.net's own market types (the
             year-long consensus trend, the peer target table, the estimate
             history with its surprises, the venue board). -->
        <dependency>
            <groupId>de.bsommerfeld</groupId>
            <artifactId>finanzen-net</artifactId>
        </dependency>
        <dependency>
            <groupId>de.bsommerfeld</groupId>
            <artifactId>tradegate</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>de.bsommerfeld</groupId>
            <artifactId>bundesanzeiger</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>de.bsommerfeld</groupId>
            <artifactId>bafin</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>de.bsommerfeld</groupId>
            <artifactId>google-news</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>dev.langchain4j</groupId>
            <artifactId>langchain4j-ollama</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.langchain4j</groupId>
            <artifactId>langchain4j</artifactId>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>
        <dependency>
            <groupId>net.sourceforge.tess4j</groupId>
            <artifactId>tess4j</artifactId>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

</project>
