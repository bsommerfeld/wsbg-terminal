package de.bsommerfeld.signals;

/** What the {@link ViewRegister} does with a signal. */
public enum SignalType {

    /** Shows the view the signal is wired to, after handing it the payload. */
    CHANGE_VIEW
}
