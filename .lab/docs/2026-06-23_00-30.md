# HANDOFF — Quality-Pass + zwei Live-Smokes + Performance-Fixes — 2026-06-23 00:30

## Kontext
Fortsetzung von `2026-06-22_14-30.md` (Plan: Resolver-Venue, Cleanup, Settings-View,
Auto-Update). Danach **zwei Live-`run.sh`-Smokes** mit User-Feedback, die echte UI-/
Performance-Bugs aufdeckten — alle gefixt. Branch `feat/editorial-lab-per-subject`.
Stand: **alles kompiliert, gesamte Testsuite grün; nichts committet.**

## Was umgesetzt ist (✅ = fertig + getestet)

### Aus dem Plan (Phasen 1–5)
- ✅ **Resolver-Venue:** `TickerResolver.preferenceRank` Venue-Tier (Suffix-basiert):
  Primär (US/`.DE`/`.L`/`.PA`/`.MI`/`.SW`…)=0, Frankfurt `.F`=+100, obskur (`.WA`)=+400.
  TTWO→Nasdaq. +3 Tests (16/16).
- ✅ **Cleanup:** toter Cluster-Pfad (`runTick`/`processCluster`/`runClusterTraced`/
  `composeOne`/`ClusterEditorial`/`EditorialListener`/`SubjectDraft`/`renderOne`/
  `appendNewsAge`), `agent/tool/`-Paket (8) + 3 Tests, Prompts `headline-compose-single`/
  `agent-system`, LabRunner-Altlasten. `PromptLoaderTest`→`headline-theme`. Behalten:
  `headline-compose.txt` (PipelineSmokeIT), Centroid (Snapshot-Shape).
- ✅ **UI-Shell:** Titel „Schlagzeilen"; Theme-Toggle→Settings-Zahnrad (Hover-Dreh);
  Settings-View ersetzt Mitte (gebogener Zurück-Pfeil, flat); `settings.css`/`settings.js`;
  `theme.js` refactored (+ System-Follow). Dark/Light-Audit: ok.
- ✅ **Settings + `SettingsBridge`** (persistiert via jshepherd): Aussehen (Switch +
  System-Opt-in), Schlagzeilen (NUR TICKER/ALLES → `cluster-theme-enabled`), Ungeprüfte
  Preise (`headlines.unverified-prices-shown`, Standard aus, „!"-Badge), Sprache, Auto-Update.
  5/5 Tests.
- ✅ **Auto-Update-Opt-out + In-App-Update:** Launcher liest `auto-update`, `--force-update`,
  gibt `WSBG_LAUNCHER_EXECUTABLE` weiter; `UpdateService` + `AppMain.relaunchForUpdate`.
  🔧 **End-to-End braucht paketierten Lauf** (dev hat keinen Launcher → Button bleibt versteckt).

### Aus den Smoke-Befunden (Post-Smoke-Fixes)
- ✅ **CSS-`hidden`-Bug** (Smoke 1): `.iconbtn{display}`/`.settings-view{display}` schlugen
  das HTML-`hidden`-Attr → grüner Button + Settings-View **permanent sichtbar/über den
  Widgets**. Fix: `.update-btn[hidden]` / `.settings-view[hidden] { display:none }`.
- ✅ **Reddit graceful fallthrough** (Smoke 1, [[feedback_reddit_graceful_fallthrough]]):
  `ScrapeStats.failed`; `FallbackRedditSource` fällt bei Scan-Fehler sofort auf RSS durch,
  demotet die kaputte Quelle **10 min**, **owns aggregate health** (Scraper mit null-Bus) →
  DEFEKT nur wenn ganze Kette unten. +4 Tests (8/8).
- ✅ **CEF-Warmup-Backoff** (Anti-„Sturm"): exponentiell bei 503/429 (2,5s→…→60s Cap) +
  `Retry-After` respektiert + 5-min-Budget statt 40 fixe Probes. Schont die API/IP.
- ✅ **Performance (Smoke 2 — „alles laggt 1–2s, sogar das GIF"):** zwei Ursachen, beide
  gefixt — [[reference_osr_java2d_perf]]:
  1. **Metal wieder Default.** `sun.java2d.metal=false` (deprecated OpenGL) bremste die
     Software-OSR-Java2D-Blits massiv. Jetzt Metal default; OpenGL nur Opt-in
     (`-Dwsbg.j2d.opengl=true` / `WSBG_J2D_OPENGL=true`).
  2. **Versteckter Fetch-Browser malte reddit.com unsichtbar mit 60fps.** Neuer
     `CefHost.createFetchBrowser` (windowless_frame_rate=1); `fetch()`/JS sind paint-unabhängig.

## Smoke-Befunde (Internals)
- Boot sauber (Ollama isoliert, Joker `browser→direct`, JCEF, Fenster, eager Singletons ok).
- **Reddit ist gerade hart IP-gedrosselt** (Dev-IP): Smoke 1 → Browser-Probe ok, aber
  `limit=50`-Scan 503; Smoke 2 → schon `limit=1`-Probe 503 → sauber auf **RSS** aufgelöst,
  **kein DEFEKT** (Fix wirkt). RSS zog 30+ Threads inkl. Bildern (`[img]`,`[img×2/3]`).
- **RSS-Fähigkeiten bestätigt:** Bilder werden extrahiert (`extractImages`, full-res i.redd.it)
  → Vision→Subjekt-Attribution funktioniert quellenunabhängig; Clustering 1:1 Thread voll
  funktionsfähig. Weg fällt nur: Kommentar-**Hierarchie** (flach), Scores, Polls, Cap ~100;
  Multi-Bild-Galerien im Feed dünner (oft nur 1 Preview).
- DEFEKT-Reststand in Smoke 2: ein echter transienter DEGRADED (auch RSS-Erstfetch 503),
  dessen Recovery-`OK` „skipped — no clients" war (lag-bedingter Socket-Drop) → blieb hängen.
  Sollte mit den Perf-Fixes (stabiler Socket) verschwinden.

## Shutdown / RAM (User-Sorge geprüft) — KEIN Problem
- **App räumt sauber auf:** kein Java/AppMain-Rest, **Port 11500 frei**, kein Ollama-Runner
  (Modell nicht mehr im RAM), keine verwaisten Renderer.
- „30 GB benutzt" = normales macOS-Caching. Real: **48 GB total, 89 % frei, 0 Swap** → null
  Druck. Top-Verbraucher waren User-Apps (IntelliJ 4,6 GB etc.).
- Der **Mac-weite Lag** des Users kam von **Claude Design** (lokales Slide-Rendering: Claude
  Helper Renderer + WebKit.GPU, Load-Avg ~13–16/16 Kerne) — unabhängig von WSBG.

## Offen / als Nächstes
- 🔧 **Smoke 3 jetzt:** Perf live gegentesten (flüssig? GIF ok?), Reddit-Fallback live,
  und — wenn die IP-Drossel weg ist — ob der Browser-Pfad + Cluster/Headlines/Resolver-Venue
  durchlaufen.
- ⏸️ In-App-Update **paketiert** verifizieren (Relaunch, sauberer Close, kein Ollama-Orphan).
- ⏸️ RAM-Folgeoptimierung: idle Fetch-Browser (~150 MB) entsorgen, wenn Quelle auf RSS demoted.
- ⏸️ Metal→OpenGL-Opt-in nur falls der seltene Sleep/Wake-Crash je real auftritt (Soak).
