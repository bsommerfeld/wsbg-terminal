# Wiedereinstieg — konsolidierter Stand & Prioritätsliste (zwei Umbauten) — 2026-06-11 02:54

## Kontext
Kontext zweier paralleler Umbauten ging durch einen Laptop-Neustart verloren.
Dieser Eintrag rekonstruiert den Stand **aus dem Code + den älteren Docs** und
fasst alle offenen Punkte zu **einer** priorisierten Liste zusammen. Branch
`feat/editorial-lab-per-subject`. Beide Umbauten liegen **uncommitted und
ineinander verwoben** im Working Tree; alles baut grün (Agent-Suite 157,
News-Module + Core grün).

## Die zwei Umbauten
- **A — Subject-Registry (B):** Editorial-Atom = feed-weite `SubjectUnit`
  (eine Headline pro Subjekt, NEW/UPDATE), Collation, News-Coverage 3b,
  Reply-Baum. **Nur im Lab** verdrahtet; Prod nutzt noch altes Cluster-`runTick`.
- **B — News-Quellen-Architektur:** `source-api` (`NewsSource`+`RawNewsItem`),
  `embedding`-Modul, `aggregator` (`NewsAggregator`+`RssFeedIndex`).
  Teilt sich in **B-Fundament** (RawNewsItem-Umstellung + embedding-Modul, fertig,
  A hängt daran) und **B-Oberbau** (Aggregator füttern+anschließen, offen).

## Schon gebaut (Stand heute, ✅)
- **A:** SubjectUnit/Registry/Attributor, `composeUnit` NEW/UPDATE, chunked
  Extraction, Salvage, Tier-2-Resolver, **News-Coverage 3b** (default-off Setting),
  **Collation-Backend** (`HeadlineCollator`), **Reply-Baum** (`CommentTree`).
- **B-Fundament:** alle Quellen liefern `RawNewsItem`; Yahoo als `NewsSource`
  gebunden; `embedding`-Modul ausgegliedert.

---

## Prioritätsliste — Hier geht's weiter

### 0. Sicherung (zuerst, risikofrei)
- ❓ **Gebauten Stand committen**, bevor weitergebaut wird (Doc `2026-06-09`
  mahnt es an). Offene Frage: in **zwei getrennten Commits** (B-Fundament, dann A)
  oder ein Sammelcommit — Trennung ist fummelig, weil A und B-Fundament in
  denselben Dateien verwoben sind.

### 1. B-Oberbau — News-Aggregator anschließen (die einzige echt offene Architektur-Front)
- ❓ **Anreicherung bei Ingest entscheiden** (die hängengebliebene Frage):
  Fn/Fj-News brauchen einen Ticker zum Indizieren. Fn hat nur ISIN, Fj nichts.
  - **A) ISIN→Ticker für Fn** — womit? (openFIGI anonym / Yahoo `v1/search` /
    statische Liste). User-Tendenz: „bei Ingest anreichern" (das *Wann*, nicht das *Womit*).
  - **B) Fj→Ticker** — billiger Symbol-Scan (`WsbgJargon`/`TickerExtractor`, kein LLM)
    vs. volle Extraction. Fj bleibt **eigenständiges Widget**, wird zusätzlich indiziert
    (Kausalitätstrait). Makro-only-Items fallen still durch.
- 🟡 Danach: `RssFeedIndex` füttern (Fn/Fj→`ingest`) + ins `Set<NewsSource>` binden.
- 🟡 Danach: `TickerResolver` auf `NewsAggregator` umstellen (per-Symbol-Pfad
  `getNewsForSymbol`). Theme/Person-Pfad (`yahoo.search().news()`) bleibt vorerst
  Yahoo (Aggregator kennt nur `newsFor(symbol)`).

### 2. Collation fertigstellen (A — drei offene Stücke)
- ❓ **Threshold 0.86 live tunen** — zu hoch = Dubletten durch, zu niedrig =
  echte Stories fälschlich gemerged.
- 🟡 **UI-Teil (Terminal)** — letzte ~3–5 Rows editierbar + `update`-Push +
  golden-shimmer-wipe. Backend-Signal `Decision.collated`+RowId ist da.
- 🟡 **Prod-Verdrahtung** — `HeadlinePublisher.offer` rufen, `new` vs `update(rowId)`
  senden (aktuell lab-only).

### 3. B-Pipeline Qualitäts-TODOs (A)
- ❓ **Highlight-Kalibrierung** — IMPORTANT überzieht bei *vergangenen* Moves
  (SNDK-Capitulation → sollte NORMAL). Prompt-Feintuning.
- 🟡 **Ähnliche Namen B** — MSCI World vs. EM matchen dieselbe Evidenz-Zeile;
  `SubjectAttributor.matchingLine` muss die *spezifischste* Zeile wählen
  (TODO-Marker im Code; „Stück 4" semantisches Zeilen-Matching via EmbeddingService).
- 🟡 **Resolver-Venue** — Heimatbörse nicht vorn (Münchener→Mailand, MSTR→München, DAX).
- 🟡 **„!"-Ungeprüft-Badge UI** — Backend `UnitDraft.unverified` ✅; Terminal-UI offen.
- ❓ **Thread-3 leere Vision** — Portfolio *mit* Text gab leere Vision; Fetch-Fehler
  vs. Modell-leer unterscheiden+loggen (`AgentBrain.describeImage`).
- ⏸️ **Watchlist/Filter** — *after-MVP*. ~40 Headlines aus einem „welche Aktie?"-Thread
  priorisieren, **nicht** wegwerfen.

### 4. Test-Lücken
- 🟡 **Subjekt-Extraktions-IT** + **Compose-IT** fehlen (Rezept im Doc `2026-06-06_16-16`,
  Konstruktoren verifiziert).
- ⏸️ **B end-to-end / NEW-UPDATE-über-Läufe / Vision→Einheit→Headline** nur manuell
  im Lab gesehen.
- ⏸️ **`TickerMatchComparisonIT` live** (`RESOLVER_LIVE=true`) → Score-Schwelle final
  (gated auf Yahoo-Cooldown).

### 5. Der große Brocken
- ❓ **Prod-Cutover** — gesamte B-Pipeline ist **lab-only**; Prod läuft über altes
  Cluster-`runTick` (`AgentCoordinator:101`). Umstieg Prod→SubjectUnit ist ein
  eigener großer Schritt (Reihenfolge: erst Lab-Pipeline „feature-complete", dann
  cutover).

## Eiserne Constraints (gelten überall)
Kein Hardcoding · nichts verschlucken (Doppler ok) · kein `Co-Authored-By` ·
Highlight = Opportunity nicht Mover · Reddit 1:1 jede Stimme (kein Skip) ·
Nutzer-Zahlen = Sentiment nicht Fakt · Yahoo nur Anreicherung, blockt nie Headlines
(derzeit IP-gebannt).

## Empfohlene Reihenfolge
**0 (sichern) → 2 (Collation fertig, der nächstliegende „echt fertig machen"-Block)
→ 1 (B-Oberbau, braucht die Anreicherungs-Entscheidung) → 3/4 (Qualität+Tests)
→ 5 (Prod-Cutover als Abschluss).**
Begründung: Collation ist am weitesten und am schnellsten „abschließbar"; B-Oberbau
braucht erst eine Entscheidung; Prod-Cutover sinnvoll erst, wenn das Lab
feature-complete ist.
