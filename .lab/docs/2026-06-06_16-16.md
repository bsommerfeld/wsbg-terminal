# Hand-off — Test-Abdeckung & Stand (KI-Einheiten, B-Pipeline)

**Branch:** `feat/editorial-lab-per-subject` · **letzter Commit:** `0e081e7` · alles gepusht, 144 Unit-Tests grün.
Dies ergänzt die laufenden Handoffs `2026-06-05_16-04.md` (B-Status) und `2026-06-06_15-00.md` (Collation).

---

## Was diese Session geschafft hat (seit dem 16-04-Handoff)
- **News-Coverage 3b**, **Collation** (Headline-Merge), **Vision-als-Evidenz** (Bild = normaler Kontext), **Name-Cleaning** (Preis-Schwanz ab), **Ungeprüft-Flag** (Nutzer-Zahlen ≠ Fakt), **Regex-Salvage** (kaputtes Compose-JSON), **429-Breaker entschärft** (un-enriched statt skip), **ähnliche Namen A** (cluster-relative Distinktivität).
- **Embedding-Abstraktion:** `EmbeddingService`-Interface + `OllamaEmbeddingService` + `FakeEmbeddingService` → Collator läuft drüber und ist **unit-getestet ohne Ollama**.
- **KI-IT-Harness:** `OllamaAvailability` (auto-gate), `LocalImageServer` (statische Bilder), `VisionIT`, `EmbeddingServiceIT`, `SyntheticThreads` (Make-up-Threads).
- **5 statische Vision-Fixtures** in `agent/src/test/resources/vision/`: `watchlist-red`, `portfolio-gains`, `single-stock-intel`, `article-marvell`, `macro-qqq`.

## Wie die Tests laufen
- **Unit (immer, `mvn test`, kein Ollama):** die ganze Logik. `FakeEmbeddingService` macht embedding-abhängige Logik testbar.
- **Integration (lokal auto, in CI skip):** `@Tag("integration")` **+** `@EnabledIf(OllamaAvailability#available)` → läuft, wenn die isolierte Ollama installiert/erreichbar ist; sonst übersprungen. Kein env-Flag.
  - `VisionIT`/`EmbeddingServiceIT` so ausführen: `mvn test -pl agent -Dtest=VisionIT -Dtest.excludedGroups=` (oder die isolierte Ollama laufen lassen + `-Dtest.excludedGroups=`).

---

## Coverage-Audit (Pipeline-Stufe → Test)
| Stufe | Logik-Unit | Modell-IT | Nur Lab |
|---|---|---|---|
| Reddit-Fetch (RSS/JSON/OAuth) | ✅ RedditScraperTest, FallbackRedditSourceTest, RssRedditScraperIT | — | |
| **Clustering** (`ClusterEngine.assign`) | ❌ **kein Unit-Test** (baut eigenes Embedding-Modell) | indirekt PipelineSmokeIT | teils |
| Cluster-Registry/Rebalance | ✅ ClusterRegistryTest, ClusterRebalancerTest, InvestigationClusterTest | — | |
| **Bildanalyse** (`describeImage`) | ⚠️ nur Resize-Mathe (AgentBrainImageProcessingTest) | ✅ **VisionIT (neu)** | |
| **Subjekt-Extraktion** (Modell) | ⚠️ nur Parse/Salvage/Clean-Helfer (EditorialAgentTest) | ❌ **noch keine IT** (Rezept unten) | ja |
| Subjekt-Attribution | ✅ SubjectAttributorTest (Wort/Vision/Distinktivität) | — | |
| Subjekt-Registry/Merge/Prune/Coverage | ✅ SubjectRegistryTest | — | |
| Ticker-Auflösung (Match/Börse) | ✅ TickerResolverTest | TickerResolverLiveIT, TickerMatchComparisonIT | |
| Yahoo-Parsing | ✅ YahooFinanceClientTest | YahooArticleLiveIT | |
| **Compose** (`composeUnit` NEW/UPDATE) | ⚠️ nur Salvage/Regex/Preis-Helfer | ❌ **noch keine IT** (Rezept unten) | ja |
| Collation | ✅ HeadlineCollatorTest (Fake) | (EmbeddingServiceIT validiert Embedder) | |
| Embedding-Verhalten | — | ✅ **EmbeddingServiceIT (neu)** | |
| HeadlineWriter-QA, ReportBuilder, Snapshots | ✅ je eigener Test | — | |
| **B end-to-end** (ingest→cluster→attribute→composeUnit→collate) | — | — | **ja, nur Lab** |

## Lücken / nur-im-Lab-getestet (ehrlich)
1. **Subjekt-Extraktions-IT** — fehlt. Rezept steht (Konstruktoren verifiziert).
2. **Compose-IT** (`composeUnit` produziert NEW/UPDATE) — fehlt.
3. **ClusterEngine** — keine Unit-Test-Logik; wird erst mit dem EmbeddingService-Seam (Stück 2) fake-testbar.
4. **B end-to-end** + **NEW/UPDATE über Läufe** + **Unverified-Flag gesamt** + **Vision→Einheit→Headline** — nur manuell im Lab gesehen.

---

## Rezept für die fehlenden ITs (Konstruktoren sind verifiziert)
```java
GlobalConfig config = new GlobalConfig();
ApplicationEventBus bus = new ApplicationEventBus();
RedditRepository redditRepo = new RedditRepository();
AgentRepository agentRepo = new AgentRepository();
AgentBrain brain = new AgentBrain(config, bus, new OllamaServerManager());
ClusterRegistry registry = new ClusterRegistry();
ClusterEngine engine = new ClusterEngine(registry, config);
EditorialAgent editorial = new EditorialAgent(brain, registry, agentRepo, redditRepo,
        bus, new I18nService(config), new YahooFinanceClient(config));
SubjectRegistry subjects = new SubjectRegistry();
// 1) SyntheticThreads.allInPoll() → Thread+Comments in redditRepo legen (Repo-API prüfen: add thread/comments)
// 2) engine.assign(thread, 0, 0, visionText) → outcome.clusterId()
// 3) editorial.attributeCluster(clusterId, subjects) → List<ResolvedSubject>
//    EXTRAKTION prüfen: assert die query()-Namen enthalten erwartete Subjekte (Yahoo darf 429en → query bleibt)
// 4) COMPOSE prüfen: editorial.composeUnit(unit) → UnitDraft, assert headline nicht leer + Subjektname drin
```
TODO offen: `RedditRepository`-API zum Einspielen von Thread+Kommentaren bestätigen (upsert/add), dann `SubjectExtractionIT` + `ComposeIT` schreiben (nutzen `SyntheticThreads` + die Vision-Fixtures über `LocalImageServer`).

## Embedding-Rollout (Stück 1 ✅, Rest offen)
- **Stück 1 ✅** EmbeddingService + Collator drauf + Fake-Unit-Test.
- **Stück 2** `ClusterEngine` auf den Service (eigenes Modell raus) → Clustering fake-testbar. *Macht zugleich Coverage-Lücke #3 zu.*
- **Stück 3** Tier 2: `TickerResolver` nutzt `bestMatch` über Yahoo-Kandidaten (braucht Yahoo, derzeit 429-geparkt).
- **Stück 4** Ähnliche Namen B: semantisches Zeilen-Matching (Abkürzungen EM↔„EM IMI") via Service. TODO-Marker steht in `SubjectAttributor`.

## Eiserne Constraints
Kein Hardcoding · nichts verschlucken (Doppler ok) · kein `Co-Authored-By` · Highlight = Opportunity nicht Mover · Reddit 1:1 jede Stimme · **Nutzer-Zahlen sind Sentiment, kein Fakt** (`feedback_user_numbers_unverified`) · Yahoo nur Anreicherung, blockt nie Headlines · Yahoo derzeit IP-gebannt (Tier 2 geparkt).

## Schlüssel-Dateien (neu/zentral)
- `agent/.../EmbeddingService.java`, `OllamaEmbeddingService.java`, `HeadlineCollator.java`
- Tests: `OllamaAvailability`, `LocalImageServer`, `VisionIT`, `EmbeddingServiceIT`, `SyntheticThreads`, `FakeEmbeddingService`, `HeadlineCollatorTest`
- Fixtures: `agent/src/test/resources/vision/*.png`
- Docs: dieses File + `2026-06-06_15-00.md` (Collation) + `2026-06-05_16-04.md` (B-Status, offene Punkte #8–10)
