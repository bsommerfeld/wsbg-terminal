# Handoff-Protokoll — #2 „Subjekt = Einheit" (B) + Resolver-Tiering

**Branch:** `feat/editorial-lab-per-subject` · **letzter Commit:** `a5f8d20` (alles gepusht)
**Stand:** Abends 2026-06-05, vor Schlafpause. Morgen weiter.

---

## Wo wir stehen — #2 (B): das Subjekt ist die Editorial-Einheit
Weg vom Cluster-als-Editorial-Atom hin zu **feed-weiten Subjekt-Einheiten**, die Evidenz
aus *allen* Threads sammeln und **pro Einheit** eine Headline schreiben. Komplett im `.lab`
gebaut (Prod-Pfad nutzt noch die alte Cluster-`runTick`).

### Fertig & committet
- **Step 1–2** (`bb4cb2f`): `SubjectUnit` (feed-weit, id = Ticker UPPER / `name:<norm>`),
  `SubjectRegistry` (@Singleton, dirty-tracking), `SubjectAttributor`
  (**Wort-Level-Matching**, signifikante Wörter + Ticker via `TickerExtractor`, **0-Evidenz-
  Subjekte werden gedroppt** = keine Phantom-Units). Cross-Thread-Akkumulation bewiesen
  (NVIDIA evidence aus 3 Threads).
- **Step 2.5** (`9cdcea4`): `SubjectRegistry.mergeIdentities()` — **konservativer** Identity-
  Merge: Name-Unit fällt nur in Ticker-Unit, wenn sie **gemeinsames Wort UND gemeinsame
  Evidenz** teilen. Verschluckt nie (Pennystocks≠MustGrow bleibt getrennt). Ticker↔Ticker nie.
- **Step 3** (`3520d48`): **Per-Einheit-Compose mit NEW/UPDATE**.
  - `EditorialAgent.composeUnit(SubjectUnit)` → `UnitDraft` (headline, isUpdate, raw, ms).
  - Prompt `agent/.../prompts/headline-compose-unit.txt` (Evidenz-Slice + Yahoo-Daten +
    **eigene bisherige Headlines** → Modell taggt NEW / UPDATE, „<Subjekt>-Update: …").
  - `SubjectUnit.UnitHeadline`-Historie; Lab streamt pro dirty Unit, speichert Headline auf
    der Unit (→ nächster Lauf sieht sie → UPDATE). Identical-text-Backstop.
- **Fix dirty/over-wake** (`a2cbbd8`): `SubjectAttributor` markiert nur dirty bei **echt neuer
  Evidenz** (vorher unconditional → ganzer Feld re-komponiert). Lab re-attribuiert nur Cluster,
  die *diesen Run* einen Thread bekamen (`touchedClusters`) — spiegelt Prod (nur geänderte
  Threads). **Live verifiziert:** dirty 5→26→3, alte Cluster „skipped", NVIDIA unverändert→UPDATE.
- **Fix context-relief** (`b9e2510`): `SubjectUnit.pruneOlderThan` / `SubjectRegistry.
  pruneContentOlderThan` — **Einheit bleibt für immer**, nur verbrauchter **Inhalt** (Evidenz +
  Headlines älter als `snapshot-ttl-minutes`, Default 60) rollt raus. Lab Phase 0 ruft's auf.

### Tests
Agent-Suite **125 grün** (neu: `SubjectRegistryTest`, `SubjectAttributorTest`, composeUnit-Pfad,
prune-Test). Plus Integrationstests siehe unten.

---

## Resolver-Matching — 3-Tier-Plan (Subjekt → Yahoo-Ticker)
**Problem:** „Amazon" wurde tickerlos, obwohl Yahoo `AMZN` liefert. Single-Token-Query gegen
Namen mit Extra-Token (`Amazon.com, Inc.`). `Amazon`-legit und `Rheiner`-Fuzzy sind strukturell
gleich → braucht externes Konfidenz-Signal. **Regel: kein Hardcoding** (keine Ticker/Name-Mappings).

**Beschlossene Reihenfolge (Fallback-Kette):**
1. **Tier 1 — Yahoo-Relevanz-Score** ✅ **FERTIG** (`6d51fa1`, davor `e728fb0` „com"-Stopwort).
   `YahooQuote.score` geparst; bei Extra-Token-Name akzeptiert wenn `score ≥ MIN_CONFIDENT_SCORE`
   (aktuell **100 000**, tunebar). Popularitäts-Gewichtung hilft: Megacaps hoch, Fuzzy niedrig.
2. **Tier 2 — Embedding-Fallback** ⏳ **TODO**. `embeddinggemma`-Cosine(Query, Name) wenn Score
   nicht greift. Fängt legit Low-Score-Small-Caps + sprachübergreifend (Münchener Rück↔Munich Re).
   Braucht DI-Wiring (Embedder in den Resolver; `strongMatch` dann nicht mehr rein statisch).
3. **Tier 3 — Stop-Wort-Liste** als **letzter** Fallback (USER-Entscheid heute Abend): nur noch
   stabile Rechtsform-Suffixe (`inc`/`ag`/…). Wenn Tier 1 **und** 2 scheitern, fasst die Liste.
   „Dann haben wir alles." → exact-equality wird die letzte Instanz, nicht die erste.

**Vergleich/Pros&Cons:** ausführlich in `.lab/docs/2026-06-05_07-35.md`.

---

## OFFEN — morgen (Reihenfolge frei, beides bereit)
### A) Resolver-Tiering abschließen
1. **Vergleichs-IT live laufen** (Sandbox kriegt 429/400, du nicht):
   ```
   RESOLVER_LIVE=true mvn test -pl agent -Dtest=TickerMatchComparisonIT -Dtest.excludedGroups=
   ```
   `TickerMatchComparisonIT` druckt pro Subjekt `old=… new=… RESCUED/REGRESSED/venue-diff` +
   Top-Kandidaten mit Scores. **Daraus:** `MIN_CONFIDENT_SCORE` final setzen + sehen, welche
   legit Namen Tier 1 noch verwirft (= Tier-2-Bedarf).
2. **Tier 2 (Embedding)** gezielt für die übrigen Fälle bauen (DI-Wiring + Threshold).
3. **Tier 3** als letzten Fallback verdrahten (exact-equality nach 1+2).

### B) Step 3b — News-Coverage (B-Core)
Pro Einheit `sourceNewsIds` der schon vertonten Yahoo-/finanznachrichten-News → `covered`
markieren → nächster Compose filtert sie raus. Verhindert zwei Headlines auf derselben News.
Gleichzeitig Basis für die **finanznachrichten-Quelle** (Modul `0b0b3eb` ist seit master-Merge
da; 10-Min-Pull-Floor beachten).

### Danach (B-Core-Reste)
- **Collation:** inline push + letzte ~3-5 Headlines **editierbar** + golden-shimmer-wipe
  (braucht Backend recent-window + UI `update`-Push). „Dokumentiere Collation" → noch offen.
- **Highlight-Kalibrierung:** IMPORTANT wird bei *vergangenen* Moves (SNDK „verkaufen nach ATH"
  = Capitulation) überzogen — sollte NORMAL sein (kein Entry mehr). Prompt-Feintuning.
- **Resolver-Venue** (separat von Tiering, kein Hardcoding): Münchener→`1MUV2.MI` (Mailand),
  MicroStrategy→`MIGA.MU` (München), DAX→`^DAX-IV`/Junk. Yahoo ordnet Heimatbörse nicht nach vorn.

---

## Eiserne Constraints (nicht brechen)
- **Kein Hardcoding** — keine Ticker/Name-Mappings; nur algorithmisch. Stop-Liste = generische
  Linguistik-Tokens, **letzter** Fallback.
- **Nichts verschlucken** — Doppler sind OK („doppelt moppelt ist trotzdem in den Daten").
- **Kein `Co-Authored-By: Claude`**-Trailer in Commits.
- **Highlight = Relevanz/Opportunity**, nicht Market-Mover; Engagement irrelevant.
- Lab = realer Pfad (kein synthetischer Scraper); `run-test.sh`; Ollama isoliert auf 11500.

## Schlüssel-Dateien
- `agent/.../{SubjectUnit,SubjectRegistry,SubjectAttributor,EditorialAgent,TickerResolver}.java`
- `agent/.../prompts/headline-compose-unit.txt`
- `.lab/.../LabRunner.java` (Phase 0 prune → 1 ingest → 3 attribute(touched) → 3.5 merge → 4 composeUnit-stream)
- `agent/src/test/.../TickerMatchComparisonIT.java`, `TickerResolverTest.java`, `SubjectRegistryTest.java`
- Docs: `.lab/docs/2026-06-05_07-35.md` (Match-Vergleich), dieses File (Handoff).
