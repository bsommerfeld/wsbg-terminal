# Quality-Pass nach dem Live-Run: Resolver-Venue, Cleanup, Settings-View, Auto-Update — 2026-06-22 14:30

## Kontext

Folge des Live-`run.sh`-Laufs (`2026-06-19_18-28.md`). Aus dessen offenen Punkten +
neuen User-Wünschen ein zusammenhängender Quality-/Feature-Pass. Der ursprüngliche
Chat zum Live-Run war verloren; Befunde aus Docs + Git-History rekonstruiert.

**Mac-Crash (Live-Run, ~3 h):** User-Einschätzung — vermutlich ist der **Mac selbst**
abgeschmiert (Display sleep/wake), nicht die App. Der `sun.java2d.metal=false`-Fix
(`7ca41d0`) bleibt als Mitigation drin (no-op auf Windows, schadet nie), Ursache aber
evtl. extern. ⏸️ Braucht Mehrstunden-Soak zur echten Bestätigung.

**Windows:** Keine Branch-Änderung bricht Windows. `sun.java2d.metal` ist dort ein
No-op (Metal nur macOS); Settings-View/Resolver/Cleanup sind plattformneutral; offener
Windows-Punkt bleibt nur das bekannte OSR-Scroll-Feintuning (config-only).

## Entscheidungen

- **Resolver-Venue: Primärbörse → Frankfurt → nie obskur** (User-Wahl). Keine
  „immer Frankfurt/EUR"-Variante.
- **Cleanup-Scope:** toten Prod-Code raus, **Roadmap behalten** (Collation, Archiv-/
  Watchlist-Bridges bleiben).
- **Settings-Umfang:** Aussehen + Schlagzeilen (gesetzt) + Ungeprüfte Preise (Standard
  aus) + Sprache + Auto-Update. **Scrollen NICHT** in die Settings (User wählte es ab).
- **Auto-Update = Opt-out** + In-App-Update: bei „aus" zeigt ein grüner Download-Button
  in der Titelleiste (neben Herz, Richtung Mitte) neue Versionen; Klick schließt sauber
  und startet den Launcher neu. Launcher gibt seinen Pfad an die App weiter.

## Baustellen / Status

- ✅ **Phase 1 — Resolver-Venue.** `TickerResolver.preferenceRank` um Venue-Tier
  erweitert (Symbol-Suffix-basiert): Primär (US no-suffix, `.DE/.L/.PA/.MI/.SW`…) = 0,
  Frankfurt (`.F` + dt. Regional) = +100, unklassifiziert/obskur (`.WA`…) = +400;
  additiv zu numeric-prefix/OTC/non-EQUITY. TTWO→Nasdaq, nicht `.WA`. +3 Tests, 16/16.
- ✅ **Phase 2 — Cleanup.** Entfernt: toter Cluster-Pfad in `EditorialAgent`
  (`runTick`, `processCluster`, `runClusterTraced`, `composeOne`, `EditorialListener`,
  `ClusterEditorial`, `SubjectDraft`, `renderOne`, `appendNewsAge`); `@Deprecated
  agent/tool/`-Paket (8 Dateien) + 3 Tests; Prompts `headline-compose-single.txt`,
  `agent-system.txt`; LabRunner-Altlasten (verwaiste Imports, `renderSubjectDraft`,
  `describeResolved`). `PromptLoaderTest` auf `headline-theme` umgehängt. 155 agent-Tests
  grün, voller Reactor grün.
  - ⏸️ **Bewusst behalten:** `headline-compose.txt` (Laufzeit-Dep von `PipelineSmokeIT`),
    `ClusterEngine`-Centroid/`EmbeddingService` (nur noch Snapshot-Shape), kein
    `runUnitTick`→`runTick`-Rename (unnötige Test-Churn).
  - 📄 **Nicht-Redundanz dokumentiert:** der „doppelte Brief-Bau" pro Tick ist KEINE
    Redundanz — `attributeCluster` baut einen **uncovered** Brief (Extraktion muss alles
    sehen), `publishClusterTheme` einen **covered** Brief (Compose respektiert Coverage,
    `33abfed`). Zusammenlegen würde die Korrektheit brechen → bleibt getrennt.
- ✅ **Phase 3 — UI-Shell.** Titel „Reddit · Schlagzeilen" → „Schlagzeilen".
  Theme-Toggle (Titlebar + Footer) → Settings-Zahnrad (`js-settings-toggle`,
  Hover-Dreh). Settings-View ersetzt die Mitte (`.main.settings-open` blendet Widgets
  aus), gebogener Zurück-Pfeil oben links, flat + viel Whitespace, `css/settings.css`
  (nur Tokens → beide Themes gedeckt). `js/chrome/settings.js` (Open/Close, Esc),
  `theme.js` refactored (wiederverwendbar + System-Follow).
  - **Dark/Light-Audit:** `tokens.css` deckt beide Themes vollständig; neue Styles nur
    via Tokens; verbleibende Hardcodes sind bewusste Brand-/OS-Chrome (macOS-Ampel,
    DE-Flagge mit eigenem Light-Override, Gold-Bullion, Yahoo-Lila). Keine Lücken.
- ✅ **Phase 4 — Settings-Inhalt + Bridge.** `SettingsBridge` (persistiert via
  jshepherd `config.save()`, broadcastet Snapshot bei Client-Open). Kategorien:
  Aussehen (custom Tag/Nacht-Switch, theme-getrieben + System-Opt-in-Checkbox, Standard
  aus → Switch dann gesperrt), Schlagzeilen (NUR TICKER/ALLES → `cluster-theme-enabled`),
  Ungeprüfte Preise (neuer Key `headlines.unverified-prices-shown`, Standard aus; „!"-
  Badge in `reddit.js`, `priceVerified` aus `HeadlineJson` abgeleitet, CSS-geschaltet),
  Sprache (DE/EN), Auto-Update. 5/5 `SettingsBridgeTest` grün.
- ✅ **Phase 5 — Auto-Update-Opt-out + In-App-Update** (implementiert, compiliert, Suite
  grün). Launcher: liest `user.auto-update` (config.toml, Opt-out; tiny line-scan wie
  `LauncherI18n`), `--force-update`-Arg überschreibt, gibt `WSBG_LAUNCHER_EXECUTABLE`
  weiter. Terminal: `UpdateService` (nur wenn auto-update aus + Launcher bekannt; pollt
  GitHub latest-Tag vs `version.txt`; broadcastet `update-available`),
  `AppMain.relaunchForUpdate` (shutdownServices → Launcher `--force-update` spawnen →
  CefHost dispose → exit; Reihenfolge löst erst den Single-Instance-Lock, damit der neue
  Launcher nicht nur das alte Fenster raised). Grüner Button + JS in Phase 3 verdrahtet.
  - 🔧 **Verifikation offen:** der End-to-End-Relaunch ist in dev (`run.sh`, kein
    Launcher) nicht ausführbar → braucht einen **paketierten** Lauf (Launcher startet
    Terminal). Bis dahin „gebaut + compile-/unit-geprüft", analog zum Browser-Joker.

## Offen / als Nächstes

- ⏸️ Paketierten Lauf machen und den In-App-Update-Flow live verifizieren (Button
  erscheint bei neuem Release; Klick schließt sauber, kein Ollama-Orphan, Launcher
  updatet + startet neu).
- ⏸️ Mehrstunden-Soak für den Metal→OpenGL-Fix.
- 📄 README/CHANGELOG für Settings-View + Auto-Update aktualisiert (Doku-Pflicht).
