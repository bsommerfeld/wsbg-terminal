/**
 * The starter: what a native launcher runs. It lays the bundled install into
 * the user's data directory and boots it from there in a module layer of its
 * own, so the install can be updated while the bundle stays untouched.
 */
module de.bsommerfeld.starter {
    exports de.bsommerfeld.starter;
}
