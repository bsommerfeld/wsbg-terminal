# Handoff-Protokoll #2 — B-Pipeline gehärtet + 429-Breaker

**Branch:** `feat/editorial-lab-per-subject` · **letzter Commit:** `0b79760` (alles gepusht)
**Supersedes:** `.lab/docs/2026-06-05_07-38.md` (Vorgänger-Handoff). Dies ist der aktuelle Stand.

---

## Wo wir stehen — #2 (B): Subjekt = Editorial-Einheit
Feed-weite `SubjectUnit`s sind das Editorial-Atom (eine Headline pro Einheit, NEW/UPDATE).
**Nur im `.lab`** verdrahtet; Prod nutzt noch die alte Cluster-`runTick`. **Live verifiziert.**

### Seit dem letzten Handoff zusätzlich fertig (alles committet)
- **Resolver „com"-Stopwort** (`e728fb0`): `Amazon`→`AMZN` löst wieder auf (Single-Token-Strict-Mode verwarf es wegen `Amazon.com`).
- **Resolver Tier 1 — Yahoo-Score** (`6d51fa1`): `YahooQuote.score` geparst; Extra-Token-Name akzeptiert wenn `score ≥ MIN_CONFIDENT_SCORE` (100 000). Ersetzt die Stop-Listen-Katz-und-Maus.
- **Vergleichs-IT** (`b44ddb8`): `TickerMatchComparisonIT` (live, `RESOLVER_LIVE=true`) — alt vs. neu Matcher über dieselben Quotes, druckt RESCUED/REGRESSED + Scores. **Noch nicht vom User live gefahren.**
- **0-Subjekt-Diagnose** (`311e0d1`): loggt Brief-Größe + rohe Antwort bei 0 Subjekten.
- **Salvage + NEW-immer-publishen** (`b2a647d`): kaputtes/abgeschnittenes Subjekt-JSON → Namen werden gerettet statt Totalverlust (`EditorialAgent.salvageSubjectNames`). Prompt `headline-compose-unit.txt`: leere Headline NUR für redundantes UPDATE; ein NEU-Subjekt (keine Vorgeschichte) kriegt IMMER eine Zeile (fix für Lenovo/MSFT-„keine Headline").
- **Chunked Extraction** (`e4dcdc0`): > `EXTRACT_CHUNK_SIZE` (25) Kommentare → Extraktion in 25er-Batches, Namen vereinigt (case-insensitive). Verhindert die malformed-Array-Degeneration des 4B-Modells (war: 0 Subjekte aus 75-Kommentar-Thread). **Kein Reddit-Sonderpull** — rein In-Memory-Read + mehrere lokale Ollama-Calls. Kleine Cluster: unverändert (1 Call, voller Rich-Brief).
- **429-Circuit-Breaker** (`0b79760`): erste 429/503/999 öffnet Breaker 90s → alle weiteren Yahoo-Calls short-circuiten (keine Kaskade). Betroffene Subjekte = `ResolvedSubject.unresolved` → `SubjectAttributor` ÜBERSPRINGT sie (keine falschen tickerlosen Einheiten, die den Merge blockieren). **Ignoriert, nicht retry'd** — Neuauflösung beim nächsten Evidenz-Eingang. Deckt search + spark-batch + single-chart.

### Tests
Agent-Suite **128 grün**. Integrationstests (gated): `TickerMatchComparisonIT`, `TickerResolverLiveIT`, `PipelineSmokeIT`.

---

## Resolver-Matching — 3-Tier-Plan
**Reihenfolge (Fallback-Kette):** Yahoo-Score → Embedding → Stop-Liste. **Kein Hardcoding.**
1. **Tier 1 — Yahoo-Score** ✅ FERTIG.
2. **Tier 2 — Embedding-Fallback** ⏳ TODO. `embeddinggemma`-Cosine wenn Score nicht greift. Braucht DI-Wiring (Embedder in Resolver). Erst `TickerMatchComparisonIT` live fahren → `MIN_CONFIDENT_SCORE` tunen + sehen, was Tier 1 verpasst.
3. **Tier 3 — Stop-Liste** = letzter Fallback (nur stabile Rechtsform-Suffixe; NICHT mehr wachsen lassen).

Pros/Cons-Doku: `.lab/docs/2026-06-05_07-35.md`.

---

## OFFEN — nächste Schritte (Reihenfolge frei)
1. **`TickerMatchComparisonIT` live** (`RESOLVER_LIVE=true mvn test -pl agent -Dtest=TickerMatchComparisonIT -Dtest.excludedGroups=`) → Score-Schwelle final + Tier-2-Bedarf klären. **(Erst Yahoo-Cooldown abwarten — wir wurden 429't.)**
2. **Tier 2 (Embedding)** bauen.
3. **Step 3b — News-Coverage** (B-Core): pro Einheit `sourceNewsIds` der vertonten Yahoo-/finanznachrichten-News → `covered` → nächster Compose filtert. Verhindert zwei Headlines auf derselben News. Basis für die **finanznachrichten**-Quelle (Modul seit master-Merge da; 10-Min-Pull-Floor).
4. **Collation** (B-Core, das „Merge ähnlicher Headlines", das der User eigentlich meinte): inline push + letzte ~3–5 Headlines **editierbar** + golden-shimmer-wipe. **Noch NICHT gebaut.** Braucht Backend recent-window + UI `update`-Push.
5. **Watchlist/Filter-Layer** — **AFTER-MVP / future work** (User-Entscheid 2026-06-06): ein „welche-Aktie"-Thread erzeugt legitim ~40 Headlines. Live inkrementell entschärft das schon; Priorisierung/Gewichtung (ab N Evidenz / nur IMPORTANT / User-Watchlist) kommt **nach** dem MVP. **Kein Hardcoding, nichts wegwerfen — nur priorisieren.**
6. **Highlight-Kalibrierung**: IMPORTANT wird bei *vergangenen* Moves (SNDK „verkaufen nach ATH" = Capitulation) überzogen → sollte NORMAL sein. Prompt-Feintuning.
7. **Resolver-Venue** (separat, kein Hardcoding): Münchener→`1MUV2.MI` (Mailand), MicroStrategy→`MIGA.MU` (München), DAX→`^DAX-IV`. Yahoo ordnet Heimatbörse nicht nach vorn.

### Geparkte Idee (low prio)
- **Einheiten-Rebalancer**: nachträgliche Umsortierung *fehlplatzierter Evidenz* zwischen Einheiten (NICHT Subjekt-Merge, NICHT der Cluster-Rebalancer). User sah bisher 100% Attributions-Genauigkeit → Relevanz fraglich. Nur falls Fehlzuordnungen auftauchen.

### Verworfen
- **ClusterRebalancer für Subjekte**: nein — Ticker-Key + `mergeIdentities` reichen; aggressiver Merge würde verschlucken.

---

## Eiserne Constraints
- **Kein Hardcoding** (keine Ticker/Name-Mappings; nur algorithmisch). Stop-Liste = generische Tokens, letzter Fallback.
- **Nichts verschlucken** — Doppler sind OK.
- **Kein `Co-Authored-By: Claude`**-Trailer.
- **Highlight = Relevanz/Opportunity**, nicht Market-Mover; Engagement irrelevant.
- **Reddit 1:1-Mirror, jede Stimme** — kein Skip-Pfad, Müll ist OK, Votes sind Sentiment nicht Filter.
- Lab = realer Pfad (kein synthetischer Scraper); `run-test.sh`; Ollama isoliert auf 11500.

## Schlüssel-Dateien
- `agent/.../{SubjectUnit,SubjectRegistry,SubjectAttributor,EditorialAgent,TickerResolver}.java`
- `agent/.../prompts/{subject-extraction,headline-compose-unit}.txt`
- `yahoo-finance/.../YahooFinanceClient.java` (Breaker + `SearchResult.rateLimited`/`throttled`), `YahooQuote.java` (`score`)
- `.lab/.../LabRunner.java` (Phase 0 prune → 1 ingest → 3 attribute(touched only) → 3.5 merge → 4 composeUnit-stream)
- Tests: `EditorialAgentTest` (salvage/distribute), `TickerResolverTest` (score/com), `SubjectRegistryTest`, `SubjectAttributorTest`, `TickerMatchComparisonIT`
- Docs: dieses File (aktueller Handoff), `2026-06-05_07-35.md` (Match-Vergleich)
